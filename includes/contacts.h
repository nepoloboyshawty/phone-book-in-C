#ifndef __CONTACTS_H__
#define __CONTACTS_H__

#include <stdio.h>

void record_contact(person_info*);
int сompare_data(const void*, const void*);
void print_contacts(void);
void show_contacts(void);
void delete_contact(void);

#endif
